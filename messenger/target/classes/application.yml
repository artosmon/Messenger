---

spring:
    rabbitmq:
      host: localhost:8080
      username: guest
      password: guest
    jpa:
      database: POSTGRESQL
      show-sql: true
      hibernate:
        ddl-auto: create
    datasource:
      url: jdbc:postgresql://localhost:5432/database?
      username: postgres
      password: 1
      driver-class-name: org.postgresql.Driver
